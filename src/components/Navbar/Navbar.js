import "./Navbar.css";

const Navbar = () => {
  return (
    <header>
      <nav>
        <a href="#">HOME</a>
        <a href="#">PRODUCTS</a>
        <a href="#">ABOUT US</a>
      </nav>
    </header>
  );
};
export default Navbar;
